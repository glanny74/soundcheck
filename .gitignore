# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variables d'environnement (clés Firebase, etc.)
# .env contient des secrets et NE doit JAMAIS être commit
.env
.env.local
.env.*.local

# Service account Firebase Admin — fichier ultra sensible (accès full admin)
# Si tu télécharges le JSON, garde-le hors du projet (ex: Downloads).
*firebase-adminsdk*.json
serviceAccount*.json

# Fichier de contexte projet (local uniquement, pas dans le repo)
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
